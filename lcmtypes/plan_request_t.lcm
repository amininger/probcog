struct plan_request_t
{
    int64_t utime;

    // Rolling ID counter to identify the request
    int8_t id;

    // User may request a plan that terminates at coordinate XYT
    double goalXYT[3];
}
