struct observations_t
{
    int64_t utime;
		int64_t soar_utime;

    // Object ID of the object the user last clicked on
    int32_t click_id;

    // Camera position info in world coordinates
    double eye[3];
    double lookat[3];
    double up[3];

    // Observed objects
    int32_t nobs;
    object_data_t observations[nobs];
}
